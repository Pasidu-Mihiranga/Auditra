
import 'package:flutter/material.dart';
import 'package:intl/intl.dart';
import '../../../../models/project_model.dart';
import '../utils/field_officer_document_manager.dart';
import 'package:auditra/screens/valuation_form_screen.dart'; // Adjust path if needed
import '../../../../services/api_service.dart'; // For _viewValuationReports logic if needed inside?
// Wait, _viewValuationReports is another method in Dashboard.
// ProjectDetailsModal calls _viewValuationReports?
// Let's check the code.
// In Step 3956 (lines 1160-1184), I see "Valuation Reports" card/button?
// Step 3956 lines 1369... showed document list.
// I need to check if ProjectDetailsModal calls explicit callbacks or handles logic.
// In previous Dashboard code, it seemed to just show details and documents.
// "Valuation Reports" might be a separate section or button.
// If it navigates to another screen or calls another method, I need to pass that callback.
// "Create Valuation Report" logic?
// I'll read _viewProjectDetails fully to know what it does.
